1. React
2. Firebase Firestore Realtime db (Socket.io pour le temps réel)
  
3. Material UI (Pour les icones)
4. React Router
5. REDUX 
6. Google Authentication


width: fit-content // Prends la largeur automatique de l'element en question 

<ChatMessage className={`${true && 'chat__reciever'}`}> [true] vient de : message.name === user.displayName

<p className={`chat__message ${true && 'chat__reciever'}`}></p>
